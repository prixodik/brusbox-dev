mixin documentationBlock(data)
	.documentation-block&attributes(attributes)
		._row
			._img
				img(src=data.img, alt="")
			._info
				h5._title
					!=data.title
				._params
					each params in data.params
						._param
							span
								!=params.param
							span._param-value
								!=params.value
							a(href="#")._param-icon
								+svg('download')

mixin documentationBlockTwoImg(data)
	.documentation-block.--two-img&attributes(attributes)
		._row
			._imgs
				img(src=data.img1, alt="")._img
				img(src=data.img2, alt="")._img
			._info
				._text
					h5._title
						!=data.title
					._subtitle
						!=data.subtitle
				._button
					a(href="#").btn.-border.-red
						!=data.btn