mixin offerBlock(data)
	.offer-block&attributes(attributes)
		._row
			._img
				+webp(data.img)
			._about
				._info
					h5._title
						!=data.title
					._subtitle
						!=data.subtitle
				._button
					a(href="#").btn.-border Подробнее о BRUSBOX ACLASS