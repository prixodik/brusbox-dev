mixin textSection(data)
	.text-section&attributes(attributes)
		._container.container.container--text
			if data.title
				h4._info-title
					!=data.title
			if data.text
				._text 
					!=data.text
			if data.btn 
				._buttons 
					a.btn.--border(href="#")
						!= data.btn
			block

mixin textSectionContainerSm(data)
	.text-section&attributes(attributes)
		._container.container.container--text-section
			if data.title
				h4._info-title
					!=data.title
			if data.text
				._text 
					!=data.text
			if data.btn 
				._buttons 
					a.btn.--border(href="#")
						!= data.btn
			block

mixin textSectionTwoCol(data)
	.text-section&attributes(attributes)
		._container.container.container--text
			._row.row 
				._col.col-6.col-sm-12
					if data.titleLeft
						h4._info-title
							!=data.titleLeft
					if data.textLeft
						._text 
							!=data.textLeft
				._col.col-6.col-sm-12
					if data.titleRight
						h4._info-title
							!=data.titleRight
					if data.textRight
						._text 
							!=data.textRight
			if data.btn 
				._buttons 
					a.btn.--border(href="#")
						!= data.btn

mixin textSectionTwoColTitleH5(data)
	.text-section&attributes(attributes)
		._container.container.container--text
			._row.row 
				._col.col-6.col-sm-12
					if data.titleLeft
						h5._info-title
							!=data.titleLeft
					if data.textLeft
						._text 
							!=data.textLeft
				._col.col-6.col-sm-12
					if data.titleRight
						h5._info-title
							!=data.titleRight
					if data.textRight
						._text 
							!=data.textRight
			if data.btn 
				._buttons 
					a.btn.--border(href="#")
						!= data.btn

mixin textSectionTwoColBorder(data)
	.text-section.--border&attributes(attributes)
		._container.container
			._row.row 
				._col.col-6.col-sm-12
					if data.titleLeft
						h4._info-title
							!=data.titleLeft
					if data.textLeft
						._text 
							!=data.textLeft
				._col.col-6.col-sm-12
					if data.titleRight
						h4._info-title
							!=data.titleRight
					if data.textRight
						._text 
							!=data.textRight

mixin textSectionTwoColBorderTitleH5(data)
	.text-section.--border&attributes(attributes)
		._container.container
			._row.row 
				._col.col-6.col-sm-12
					._info
						if data.titleLeft
							h5._info-title
								!=data.titleLeft
						if data.textLeft
							._text 
								!=data.textLeft
				._col.col-6.col-sm-12
					._info
						if data.titleRight
							h5._info-title
								!=data.titleRight
						if data.textRight
							._text 
								!=data.textRight

mixin textSectionTitle(data)
	.text-section.--center.--title-gray&attributes(attributes)
			h3._title
				!=data.title

mixin textSectionLong(data)
	.text-section.--long&attributes(attributes)
		._container.container.container--sm
			if data.title
				h4._info-title
					!=data.title
			if data.text
				._text 
					!=data.text